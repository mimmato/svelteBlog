// mdsvex.config.js
import rehypeSlug from 'rehype-slug';
import { mdsvex } from 'mdsvex';

const config = {
  extensions: ['.svelte.md', '.md', '.svx'],
  layout: {
    posts: './src/mdsvex.svelte'
  },
  rehypePlugins: [rehypeSlug]
};

export default config;
